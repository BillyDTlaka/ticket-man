[build]
builder = "nixpacks"
buildCommand = "cd api && npm install && npm run build"

[deploy]
startCommand = "cd api && npx prisma migrate deploy --schema src/prisma/schema.prisma && node dist/server.js"
